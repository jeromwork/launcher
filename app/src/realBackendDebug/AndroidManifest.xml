<?xml version="1.0" encoding="utf-8"?>
<!--
  realBackend + debug variant overlay (F-5b E2E).
  Wires the cleartext whitelist for Firebase Emulator on 127.0.0.1 / 10.0.2.2
  / localhost so CloudConfigEncryptionE2ETest can talk to the local emulator
  over plain HTTP. Stripped from release builds.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:networkSecurityConfig="@xml/network_security_config"
        tools:targetApi="24" />
</manifest>
