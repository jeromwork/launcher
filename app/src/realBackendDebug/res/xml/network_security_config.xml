<?xml version="1.0" encoding="utf-8"?>
<!--
  F-5b E2E (CloudConfigEncryptionE2ETest, debug-only overlay):

  Firebase Emulator listens on plain HTTP. On a USB-connected real device we
  route Firestore + Auth to `127.0.0.1` via `adb reverse tcp:8080 tcp:8080`
  and `tcp:9099 tcp:9099`. AVDs use `10.0.2.2`. Both hosts need cleartext
  whitelisting so the SDK's HTTP-to-emulator handshake is not blocked by the
  Android cleartext-traffic policy.

  This file is in the `realBackendDebug` sourceSet — it is merged ONLY into
  the realBackend-debug variant. Release builds (realBackendRelease) keep
  the strict default that forbids cleartext.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">127.0.0.1</domain>
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
