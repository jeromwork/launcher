{
  "name": "launcher-backup-v1",
  "version": "0.1.0",
  "private": true,
  "description": "Recovery-key-backup Worker — R2-backed storage for RecoveryKeyBackupBlob. Spec task-6 F-5 (Root Key Hierarchy + Owner Recovery). Endpoints POST /backup, GET /backup/:stableId, DELETE /backup/:stableId. Reuses @familycare/auth-jwt for Firebase ID-token verification.",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@familycare/auth-jwt": "file:../_shared/auth-jwt",
    "jose": "^5.9.6"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.5.40",
    "@cloudflare/workers-types": "^4.20241218.0",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8",
    "wrangler": "^3.99.0"
  }
}
