# Implementation Tasks: TASK-122 F-CRYPTO Rust FFI Foundation

**Spec**: [spec.md](./spec.md) | **Plan**: [plan.md](./plan.md) | **Branch**: `task-122-crypto-ffi-foundation`

> **Spec Kit phase**: `/speckit.tasks` output. Each task traces back to spec FR/NFR/SC/US or plan §Phase. Tasks marked `[P]` are parallel-safe (different files, no shared state). Tasks marked `[deferred-<type>]` cannot be closed inside the AI session (require physical device / local emulator that the AI cannot spin up) and are surfaced by `pre-pr-backlog-sync` as `[auto:deferred-*]` backlog AC.

## Assumptions locked from Clarifications (do not re-litigate)

- **Q1**: UniFFI 0.28 **proc-macro** interface (`#[uniffi::export]`, `#[uniffi::setup_scaffolding]`). **No `.udl` file anywhere in this task**.
- **Q2**: Local desktop verification only — **no GitHub Actions workflow**, no self-hosted runner.
- **Q3**: Rust `1.97.0` pinned via `rust-toolchain.toml`. `Cargo.toml` has no `rust-version` (MSRV concept N/A here).
- **Q4**: Panic-across-FFI smoke test is mandatory. `panics()` exported + `panic_isConvertedToKotlinException` androidTest.
- **Q5**: **arm64-v8a only** on first release. armv7 / x86 / x86_64 not built in this task.

Any task suggesting `.udl`, GitHub Actions, x86 build, or Rust version other than 1.97.0 is a scope violation — refuse.

---

## Phase 2.1 — Rust workspace scaffold

Purpose: create the standalone Rust crate skeleton so `cargo check` succeeds against an empty `lib.rs`. No Gradle, no UniFFI yet.

- [ ] **T001** Create `crypto-ffi/Cargo.toml` — single-crate manifest (no `[workspace]`), `[package] name = "crypto_ffi"`, `edition = "2021"`, `[lib] crate-type = ["cdylib", "staticlib"]`, `[dependencies] uniffi = { version = "0.28", features = ["cli"] }`, `[build-dependencies] uniffi = { version = "0.28", features = ["build"] }`. **Files**: `crypto-ffi/Cargo.toml` (new). **Verify**: `cd crypto-ffi && cargo check` errors only on missing `src/lib.rs` (expected). (Plan §Phase 2.1, FR-001, NFR-002)

- [ ] **T002** [P] Create `crypto-ffi/rust-toolchain.toml` pinning Rust `1.97.0` with `targets = ["aarch64-linux-android"]` and `components = ["rustfmt", "clippy"]`. **Files**: `crypto-ffi/rust-toolchain.toml` (new). **Verify**: `rustup show` inside `crypto-ffi/` reports `1.97.0` as active toolchain. (NFR-001, Clarifications Q3, Q5)

- [ ] **T003** [P] Append `.gitignore` entries at repo root: `/crypto-ffi/target/`, `/crypto-ffi/build/`, `/crypto-ffi/**/*.so`, `/crypto-ffi/src/main/jniLibs/`. **Do NOT** ignore `Cargo.lock`. **Files**: `.gitignore` (edit). **Verify**: `git check-ignore crypto-ffi/target/foo` returns match; `git check-ignore crypto-ffi/Cargo.lock` returns no match. (NFR-002, NFR-003, NFR-004)

### Checkpoint Phase 2.1

`cargo check` in `crypto-ffi/` reports only missing `src/lib.rs`. Toolchain pinned. Ignores in place.

---

## Phase 2.2 — UniFFI proc-macro Rust source (hello + panics)

Purpose: implement the two exported functions. `cargo build` succeeds host-side (before Android cross-compile).

- [ ] **T004** Create `crypto-ffi/src/lib.rs` with `uniffi::setup_scaffolding!();` at top, `#[uniffi::export] pub fn hello(name: String) -> String { format!("Hello, {}", name) }`, and `#[uniffi::export] pub fn panics(msg: String) -> String { panic!("{}", msg) }`. Return type on `panics` is a formal contract only — function never returns. **Files**: `crypto-ffi/src/lib.rs` (new). **Verify**: `cd crypto-ffi && cargo build --lib` green on host target. (FR-001, FR-002, FR-011, Plan §Phase 2.2)

- [ ] **T005** [P] Create `crypto-ffi/src/bin/uniffi-bindgen.rs` with the canonical UniFFI 0.28 entry point: `fn main() { uniffi::uniffi_bindgen_main() }`. This is the binary invoked by Gradle to emit Kotlin bindings. **Files**: `crypto-ffi/src/bin/uniffi-bindgen.rs` (new). **Verify**: `cargo run --bin uniffi-bindgen -- --help` prints bindgen usage. (FR-005, Clarifications Q1)

- [ ] **T006** Commit `crypto-ffi/Cargo.lock` (auto-generated by first `cargo build`). Do NOT hand-edit. **Files**: `crypto-ffi/Cargo.lock` (new). **Verify**: `git ls-files crypto-ffi/Cargo.lock` prints the path. (NFR-002)

### Checkpoint Phase 2.2

Rust crate builds host-side. `panics()` and `hello()` compile. `uniffi-bindgen` binary works.

---

## Phase 2.3 — Gradle `:crypto-ffi` module + cargo-ndk cross-compile

Purpose: wire Android Gradle Plugin + cargo-ndk plugin so `./gradlew :crypto-ffi:build` produces `libcrypto_ffi.so` under `arm64-v8a/`.

- [ ] **T007** Create `crypto-ffi/build.gradle.kts` applying `com.android.library` + `org.mozilla.rust-android-gradle.rust-android` (or `com.github.willir.rust.cargo-ndk-android` — pick plugin per README research), configure `abiFilters = setOf("arm64-v8a")`, `minSdk = 26`, `compileSdk = 34`, cargo config `module = "."`, `libname = "crypto_ffi"`, `targets = listOf("arm64")`. Add `implementation("net.java.dev.jna:jna:5.14.0@aar")`. **Files**: `crypto-ffi/build.gradle.kts` (new). **Verify**: `./gradlew :crypto-ffi:tasks` lists cargo-ndk tasks. (FR-003, FR-004, Plan §Phase 2.3, Clarifications Q5)

- [ ] **T008** [P] Create `crypto-ffi/src/main/AndroidManifest.xml` with empty `<manifest package="family.launcher.cryptoffi"/>`. **Files**: `crypto-ffi/src/main/AndroidManifest.xml` (new). **Verify**: `./gradlew :crypto-ffi:preBuild` green. (Plan §Project Structure)

- [ ] **T009** Add `include(":crypto-ffi")` to `settings.gradle.kts`. If cargo-ndk plugin needs classpath declaration, add to root `build.gradle.kts` or `pluginManagement` block. **Files**: `settings.gradle.kts` (edit), `build.gradle.kts` root (edit if needed). **Verify**: `./gradlew projects` lists `:crypto-ffi`. (Plan §Project Structure)

- [ ] **T010** Add Gradle task `generateUniffiBindings` that runs `cargo run --bin uniffi-bindgen generate --library <path-to-libcrypto_ffi.so> --language kotlin --out-dir build/generated/kotlin/uniffi/` and wire it as dependency of `preBuild`. Register `build/generated/kotlin/uniffi/` into `android.sourceSets["androidTest"].kotlin`. **Files**: `crypto-ffi/build.gradle.kts` (edit). **Verify**: after `./gradlew :crypto-ffi:generateUniffiBindings` file `crypto-ffi/build/generated/kotlin/uniffi/crypto_ffi/crypto_ffi.kt` exists and declares `fun hello(name: String): String`. (FR-005)

- [ ] **T011** Run `./gradlew :crypto-ffi:build` locally and confirm `libcrypto_ffi.so` appears under `crypto-ffi/build/intermediates/**/arm64-v8a/`. **Files**: none (build artefact). **Verify**: `find crypto-ffi/build -name libcrypto_ffi.so` prints ≥1 path under `arm64-v8a`. (SC-001, FR-003, FR-004)

### Checkpoint Phase 2.3

`./gradlew :crypto-ffi:build` green on Windows dev-machine. `.so` produced under arm64-v8a. Kotlin binding generated.

---

## Phase 2.4 — androidTest suite (hello + panic)

Purpose: prove the FFI actually works on a real arm64 target. AI session drafts the tests but **cannot execute them** — owner runs on Xiaomi 11T or arm64 emulator (deferred markers below).

- [ ] **T012** [P] Create `crypto-ffi/src/androidTest/kotlin/family/launcher/cryptoffi/HelloFfiTest.kt` — instrumented test class calling `uniffi.crypto_ffi.hello("world")` and asserting `== "Hello, world"`. Include `@RunWith(AndroidJUnit4::class)`. **Files**: `crypto-ffi/src/androidTest/kotlin/family/launcher/cryptoffi/HelloFfiTest.kt` (new). **Verify (AI)**: `./gradlew :crypto-ffi:compileArm64V8aDebugAndroidTestKotlin` green. Execution deferred. (FR-006, SC-002, US1)

- [ ] **T013** [P] Create `crypto-ffi/src/androidTest/kotlin/family/launcher/cryptoffi/PanicFfiTest.kt` — instrumented test class with `@Test fun panic_isConvertedToKotlinException()` wrapping `uniffi.crypto_ffi.panics("test-msg")` in `assertFailsWith<Exception>` (or `assertThrows` per project convention). After the assertion also invoke `hello("still-alive")` — proves the process didn't die. **Files**: `crypto-ffi/src/androidTest/kotlin/family/launcher/cryptoffi/PanicFfiTest.kt` (new). **Verify (AI)**: compile green; execution deferred. (FR-011, SC-003, Clarifications Q4)

- [ ] **T014** Add androidTest dependencies to `crypto-ffi/build.gradle.kts`: `androidTestImplementation("androidx.test.ext:junit:1.1.5")`, `androidTestImplementation("androidx.test:runner:1.5.2")`, `androidTestImplementation("net.java.dev.jna:jna:5.14.0@aar")` (runtime for generated binding). Set `defaultConfig.testInstrumentationRunner = "androidx.test.runner.AndroidJUnitRunner"`. **Files**: `crypto-ffi/build.gradle.kts` (edit). **Verify**: `./gradlew :crypto-ffi:assembleAndroidTest` green — APK for instrumented tests built. (FR-006, FR-011)

- [ ] **T015** [deferred-physical-device] **Owner-run** `./gradlew :crypto-ffi:connectedAndroidTest` against Xiaomi 11T via USB. Both tests must be green. AI cannot execute — owner runs on desktop-PC with device attached. **Verify**: test report `crypto-ffi/build/reports/androidTests/connected/index.html` shows 2/2 green. (SC-002, SC-003, US1, Clarifications Q2)

- [ ] **T016** [deferred-local-emulator] **Owner-run** (optional fallback) `./gradlew :crypto-ffi:connectedAndroidTest` against locally-started arm64 AVD (slow on Intel host — accepted trade-off per Q5). Same acceptance as T015. **Verify**: same 2/2 green report on emulator. Only required if T015 device unavailable. (SC-002, SC-003, Clarifications Q2, Q5)

### Checkpoint Phase 2.4

AI session: androidTest classes compile + APK builds. Owner (post-merge, in Verification status): 2/2 tests green on Xiaomi 11T OR arm64 emulator.

---

## Phase 2.5 — Fitness function `verifyUniffiVersions`

Purpose: catch version drift between Rust `uniffi` crate and Kotlin runtime binding at build-time instead of runtime crash.

- [ ] **T017** Add Gradle task `verifyUniffiVersions` to `crypto-ffi/build.gradle.kts`. Task logic:
  1. Read `uniffi` version from `crypto-ffi/Cargo.toml` (parse via regex on `uniffi = "X.Y"` line under `[dependencies]`).
  2. Read `uniffi` version from `crypto-ffi/Cargo.lock` (authoritative resolved version).
  3. Read Kotlin runtime version by running `cargo run --bin uniffi-bindgen -- --version` OR by comparing against the `crypto_ffi.kt` generated file's declared version comment.
  4. Fail with actionable message if any of the three disagree: `"UniFFI version mismatch: Cargo.toml=X, Cargo.lock=Y, bindgen=Z. All three must match. See crypto-ffi/README.md § UniFFI bump."`.
  Wire task into `tasks.named("check") { dependsOn("verifyUniffiVersions") }`. **Files**: `crypto-ffi/build.gradle.kts` (edit). **Verify**: `./gradlew :crypto-ffi:verifyUniffiVersions` green in normal state. (FR-008, US3, SC-005, Plan §Phase 2.5)

- [ ] **T018** Manual mismatch trial: temporarily edit `crypto-ffi/Cargo.toml` to `uniffi = "0.27"`, run `./gradlew :crypto-ffi:verifyUniffiVersions`, confirm task fails with the actionable message from T017. **Revert edit** after confirmation. Record proof in commit message ("verified fitness function fires on artificial mismatch"). **Files**: none (transient edit reverted). **Verify**: build log shows expected FAIL then, after revert, PASS. (SC-005, US3)

### Checkpoint Phase 2.5

`verifyUniffiVersions` wired into `:crypto-ffi:check`. Artificial mismatch proven to fire.

---

## Phase 2.6 — Documentation

Purpose: enable a next developer / future crypto task author to add a new Rust FFI function without re-inventing the toolchain knowledge.

- [ ] **T019** Create `crypto-ffi/README.md` covering: (a) prerequisites (link to `docs/dev/rust-setup.md`), (b) **add-function walk-through** (edit `lib.rs`, add `#[uniffi::export] pub fn foo(...) -> ...`, run `./gradlew :crypto-ffi:build`, use `uniffi.crypto_ffi.foo(...)` from Kotlin), (c) **rebuild** (`./gradlew :crypto-ffi:build`, `./gradlew :crypto-ffi:connectedAndroidTest`), (d) **UniFFI bump procedure** (bump Cargo.toml, run `cargo update -p uniffi`, run `verifyUniffiVersions`, expected failure on Kotlin runtime side, update JNA-if-needed, re-run, commit `Cargo.lock`), (e) **panic contract note** (link to `.claude/skills/crypto-ffi-panic-check/`), (f) **Windows long-path caveat**. Target: developer unfamiliar with UniFFI can add a function in ≤30 min. **Files**: `crypto-ffi/README.md` (new). **Verify**: owner reads through, confirms walk-through is followable. (FR-009, SC-004)

- [ ] **T020** [P] Create `docs/dev/rust-setup.md` — onboarding page. Content: (a) Windows: link to `.claude/skills/rust-android-setup/SKILL.md` (automated bootstrap), (b) macOS/Linux: manual steps (`rustup default 1.97.0`, `rustup target add aarch64-linux-android`, `cargo install cargo-ndk --version 4.1.2`, install Android NDK via Android Studio SDK Manager, set `ANDROID_NDK_HOME`), (c) verification `cargo --version` + `./gradlew :crypto-ffi:build`. **Files**: `docs/dev/rust-setup.md` (new). **Verify**: file exists, links resolve. (FR-010)

- [ ] **T021** [P] Update `docs/architecture/crypto.md` frontmatter (or add a short paragraph in the AI-TLDR section) recording the UniFFI exit-ramp: `manual JNI via cbindgen, ~2-3 weeks per exported crate`. Do NOT rewrite the doc; a 2-3 line addition suffices. **Files**: `docs/architecture/crypto.md` (edit). **Verify**: `grep -n "cbindgen" docs/architecture/crypto.md` returns ≥1 hit. (Plan §Complexity Tracking, CLAUDE.md rule 3)

### Checkpoint Phase 2.6

README + rust-setup + exit-ramp record all present.

---

## Phase 2.7 — Verification workflow & backlog sync

Purpose: transition backlog state per CLAUDE.md status workflow. AI opens PR in `In Progress`, `pre-pr-backlog-sync` promotes to `Verification`, owner closes deferred AC → `Done`.

- [ ] **T022** Run `.claude/skills/crypto-ffi-panic-check/SKILL.md` locally against branch — must confirm `panics()` present in `lib.rs` and `panic_isConvertedToKotlinException` present in androidTest. **Files**: none. **Verify**: skill returns PASS. (FR-011, Plan §Test Strategy)

- [ ] **T023** Run `.claude/skills/pre-pr-backlog-sync/SKILL.md` on `task-122`. Skill regenerates `[auto:checklist]` + `[auto:deferred-*]` AC in `backlog/tasks/task-122 - F-CRYPTO-Rust-FFI-Foundation.md`. Given T015+T016 are `[deferred-physical-device]` / `[deferred-local-emulator]`, skill sets task status to `Verification` with note `pending: T015 Xiaomi 11T smoke, T016 optional arm64 emulator smoke`. **Files**: `backlog/tasks/task-122 - F-CRYPTO-Rust-FFI-Foundation.md` (edit via skill). **Verify**: task frontmatter `status: Verification`; description contains SECTION:VERIFICATION_PENDING block. (Plan §Rollout, CLAUDE.md Portfolio tracker workflow)

- [ ] **T024** [deferred-external] Open PR via `gh pr create` targeting `main`. PR body includes: (a) summary of Phase 2.1-2.6 commits, (b) `Backlog: task-122 → Verification`, (c) `pending AC: #<N> (auto:deferred-physical-device), #<M> (auto:deferred-local-emulator)`. **Files**: none (GitHub). **Verify**: PR URL returned; PR body contains Backlog line. (Plan §Rollout, CLAUDE.md branching)

- [ ] **T025** [deferred-physical-device] **Owner-run after merge**: physical smoke on Xiaomi 11T → transitions T015 to `[x]`. Re-run `pre-pr-backlog-sync` — if all AC green, task goes `Verification → Done`. **Files**: `backlog/tasks/task-122 - F-CRYPTO-Rust-FFI-Foundation.md` (edit via skill). **Verify**: task frontmatter `status: Done`. (SC-008, Clarifications Q2)

### Checkpoint Phase 2.7

Backlog reflects reality — task in `Verification` at PR merge; `Done` only after owner closes physical-device AC.

---

## Out-of-scope (explicitly NOT tasks here)

Reminder to future AI sessions — if the temptation to add these arises during implementation, refuse and note in commit message that these belong elsewhere:

- armv7 (`armeabi-v7a`) / x86 / x86_64 ABI addition → separate task after arm32 phone purchase (Clarifications Q5).
- GitHub Actions workflow → owner rejected per Clarifications Q2.
- `.udl` file for UniFFI → owner rejected per Clarifications Q1.
- Domain ports `CryptoPort` / `GroupPort` / `KeyPackagePort` → TASK-123.
- openmls integration → TASK-124.
- snow (Noise pairing) → TASK-67.
- SQLCipher persistence → TASK-125.
- iOS cross-compile → TASK-26.
- Rust version bump procedure task → created when first bump needed.

---

## Traceability matrix

| Artifact ID | Covered by tasks |
|---|---|
| **FR-001** (crypto-ffi/ + proc-macro UniFFI) | T001, T004 |
| **FR-002** (`hello(name) -> String`) | T004, T012 |
| **FR-003** (arm64-v8a build + expandable ABI) | T007, T011 |
| **FR-004** (jniLibs/<abi>/ layout) | T007, T011 |
| **FR-005** (Kotlin binding generated) | T005, T010 |
| **FR-006** (androidTest calls `hello("world")`) | T012, T015 |
| **FR-007** (local desktop verification, no GH Actions) | T015, T023, T025 |
| **FR-008** (fitness function version lockstep) | T017, T018 |
| **FR-009** (README with add-function walk-through) | T019 |
| **FR-010** (docs/dev/rust-setup.md onboarding) | T020 |
| **FR-011** (`panics()` + panic androidTest + skill) | T004, T013, T015, T022 |
| **NFR-001** (rust-toolchain.toml pinned 1.97.0) | T002 |
| **NFR-002** (Cargo.lock committed) | T003, T006 |
| **NFR-003** (generated bindings gitignored) | T003 |
| **NFR-004** (.so gitignored) | T003 |
| **SC-001** (build .so green on Windows dev) | T011 |
| **SC-002** (`hello_returnsGreeting` green on device) | T015, T016 |
| **SC-003** (panic converted to Kotlin exception) | T013, T015 |
| **SC-004** (README add-function ≤30 min) | T019 |
| **SC-005** (fitness function catches artificial mismatch) | T017, T018 |
| **SC-006** (full build ≤10 min on clean machine) | T011 (measure), T020 (setup doc enables clean-machine baseline) |
| **SC-007** (incremental ≤60 s) | T011 (measure) |
| **SC-008** (backlog Verification → Done transition) | T023, T025 |
| **US1** (Kotlin calls Rust hello via FFI) | T004, T010, T012, T015 |
| **US2** (CI validates on each PR) | **N/A per Clarifications Q2** — replaced by owner-driven verification T023-T025 |
| **US3** (fitness catches UniFFI drift) | T017, T018 |
| **Clarifications Q1** (proc-macro not .udl) | T004, T005 (no `.udl` file created) |
| **Clarifications Q2** (local only) | T015, T016, T023, T025 (no `.github/workflows/*.yml` created) |
| **Clarifications Q3** (Rust 1.97.0 pinned) | T002 |
| **Clarifications Q4** (panic contract) | T004, T013, T022 |
| **Clarifications Q5** (arm64-v8a only) | T007 (abiFilters), T002 (targets), T011 |

**Coverage**: FR-001..FR-011 all covered ✓. NFR-001..NFR-004 all covered ✓. SC-001..SC-008 all covered ✓. Clarifications Q1..Q5 all respected ✓. US2 explicitly re-mapped to owner-run verification per Q2 — this is a spec-level scope reduction agreed during Clarifications, not a coverage gap.

---

## Deferred markers summary (for `pre-pr-backlog-sync`)

| Marker | Tasks |
|---|---|
| `[deferred-physical-device]` | T015, T025 |
| `[deferred-local-emulator]` | T016 |
| `[deferred-external]` | T024 |

---

## Novice TL;DR (по-русски)

**Что тут внутри.** 25 атомарных задач (T001-T025), каждая = один коммит. Разбиты на 7 фаз:

- **Phase 2.1 (T001-T003)** — создать пустой Rust-крейт `crypto-ffi/`, запинить Rust 1.97.0, настроить .gitignore.
- **Phase 2.2 (T004-T006)** — написать `lib.rs` с двумя функциями (`hello`, `panics`), добавить `uniffi-bindgen` бинарь, закоммитить `Cargo.lock`.
- **Phase 2.3 (T007-T011)** — создать Gradle-модуль `:crypto-ffi`, подключить cargo-ndk plugin, собрать `libcrypto_ffi.so` под arm64-v8a, сгенерировать Kotlin-биндинги.
- **Phase 2.4 (T012-T016)** — написать два androidTest'а (`HelloFfiTest`, `PanicFfiTest`); прогон на Xiaomi 11T / arm64-эмуляторе — уже руками владельца (deferred, AI не сможет).
- **Phase 2.5 (T017-T018)** — Gradle-таска `verifyUniffiVersions` (fitness function): падает если версии UniFFI разошлись; проверить через искусственный mismatch.
- **Phase 2.6 (T019-T021)** — README (как добавить новую Rust-функцию за 30 мин), `docs/dev/rust-setup.md`, exit-ramp запись в `docs/architecture/crypto.md`.
- **Phase 2.7 (T022-T025)** — прогнать skill `crypto-ffi-panic-check`, вызвать `pre-pr-backlog-sync`, открыть PR (backlog переходит в `Verification`); после merge и прогона на Xiaomi 11T — `Done`.

**Что помечено `[deferred-*]`**: T015 и T025 (нужен Xiaomi 11T физически), T016 (нужен arm64 эмулятор — медленный на Intel, опционален), T024 (открытие PR — external step владельца). AI не сможет это закрыть в сессии — `pre-pr-backlog-sync` автоматически переведёт backlog-task в `Verification` при PR merge, `Done` — только после того как владелец прогонит на устройстве.

**Что НЕ делаем в этой таске** (напоминание чтобы будущие AI не начали дописывать scope): armv7 сборку, GitHub Actions, `.udl` файл, домейн-порты, openmls, snow, SQLCipher, iOS. Всё это — отдельные будущие таски.
