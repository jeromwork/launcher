# Preset / Profile / Component Architecture → moved to `ecs.md`

**This document has moved.** The canonical, ecosystem-wide configuration model — Entity · Component · System, the runtime invariants (self-containment, Profile+Preset pairing), class/app architecture, the adopted-approach beacon, and industry grounding — now lives in a single source of truth:

## → [`docs/architecture/ecs.md`](ecs.md)

It is named `ecs.md` (not `preset-model.md`) because the approach spans every ecosystem app, not just presets.

Do **not** re-add model content here. Edit `ecs.md` and keep other docs pointing to it (see `ecs.md` §12, sync rule). The `ecs` skill routes agents there automatically.
