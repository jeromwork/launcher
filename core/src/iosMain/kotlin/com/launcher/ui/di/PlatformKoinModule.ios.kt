package com.launcher.ui.di

import org.koin.dsl.module

/**
 * iOS placeholder. Populated when iOS bootstrap spec runs.
 */
val iosPlatformModule = module {
    // Empty until iOS spec ships actuals for AppIndex, SystemEventBridge, etc.
}
