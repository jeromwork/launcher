# push-worker — Cloudflare Worker для FCM push relay
# Spec 007 §FR-019..025
#
# TODO (Phase 5, T061-T070): полная реализация согласно plan.md
# TODO (C10 exit ramp): при миграции на свой домен — добавить routes block ниже.
#   См. push-worker/README.md §Migration to custom domain (будет создан в T070).
#
# name: ёлжно быть уникальным per Cloudflare account.
# main: точка входа; заменяется на src/index.ts когда добавим TypeScript build в T061.
# compatibility_date: Cloudflare API compat date — стандарт 2025-05-11.

name = "launcher-push"
main = "src/index.js"
compatibility_date = "2025-05-11"
account_id = "c8f9c8c59e930e0283d713b91c01fb13"

[vars]
# Public (не secret) — Firebase project ID для FCM HTTP v1 send API.
FIREBASE_PROJECT_ID = "launcher-old-dev"

# routes — раскомментировать при миграции на custom domain (TODO-ARCH-001 backlog).
# [[routes]]
# pattern = "push.<our-domain>/notify"
# custom_domain = true
